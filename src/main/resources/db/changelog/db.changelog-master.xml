<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="001-create-app-users" author="codex">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="app_users"/>
            </not>
        </preConditions>
        <createTable tableName="app_users">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="username" type="VARCHAR(255)">
                <constraints nullable="false" unique="true"/>
            </column>
        </createTable>
    </changeSet>

    <changeSet id="002-create-tasks" author="codex">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="tasks"/>
            </not>
        </preConditions>
        <createTable tableName="tasks">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="title" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="description" type="TEXT"/>
            <column name="status" type="VARCHAR(255)" defaultValue="TODO">
                <constraints nullable="false"/>
            </column>
            <column name="category" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="user_id" type="BIGINT">
                <constraints nullable="false"
                             foreignKeyName="fk_tasks_user"
                             references="app_users(id)"/>
            </column>
        </createTable>
    </changeSet>

    <changeSet id="003-replace-completed-with-status" author="codex">
        <preConditions onFail="MARK_RAN">
            <columnExists tableName="tasks" columnName="completed"/>
        </preConditions>
        <sql>
            ALTER TABLE tasks ADD COLUMN status VARCHAR(255);
            UPDATE tasks
            SET status = CASE WHEN completed THEN 'DONE' ELSE 'TODO' END;
            ALTER TABLE tasks ALTER COLUMN status SET NOT NULL;
            ALTER TABLE tasks DROP COLUMN completed;
        </sql>
    </changeSet>

</databaseChangeLog>
