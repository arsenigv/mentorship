<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="002-init-tasks" author="codex">
        <createTable tableName="tasks">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="title" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="description" type="TEXT"/>
            <column name="status" type="VARCHAR(255)" defaultValue="TODO">
                <constraints nullable="false"/>
            </column>
            <column name="category" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="user_id" type="BIGINT">
                <constraints nullable="false"
                             foreignKeyName="fk_tasks_user"
                             references="app_users(id)"/>
            </column>
        </createTable>

        <sql>
            ALTER TABLE tasks
                ADD CONSTRAINT chk_tasks_status
                CHECK (status IN ('TODO', 'IN_PROGRESS', 'DONE'));
            ALTER TABLE tasks
                ADD CONSTRAINT chk_tasks_category
                CHECK (category IN ('HOME', 'WORK', 'EDUCATION', 'OTHER'));
        </sql>

        <createIndex tableName="tasks" indexName="idx_tasks_status">
            <column name="status"/>
        </createIndex>
        <createIndex tableName="tasks" indexName="idx_tasks_user_id">
            <column name="user_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
